The taxonomic rank (a key way to classify biological organisms) can be represented as an array and should include 8 ranks in the following order:

1. domain
2. kingdom
3. phylum
4. class
5. order
6. family
7. genus
8. species

The array `ranks` that is provided is missing a few (and has one defined incorrectly!)

 

Using only the `splice()` method, amend the array provided below to correct these errors.

**Hint:** You'll need to do the following:

- Remove the incorrect item "category"
- Add the missing ones in the middle, including "phylum", "class", and "order"
- Add the missing one to the end, "species"

Then, printing your final `ranks` array to the console when you're done to see that it contains the proper values!

